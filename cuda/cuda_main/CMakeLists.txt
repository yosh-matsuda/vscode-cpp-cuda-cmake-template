project(cuda_main CXX CUDA)

file(
  GLOB_RECURSE CUDAMAIN_SOURCE_FILES
  RELATIVE ${CMAKE_CURRENT_SOURCE_DIR}
  CONFIGURE_DEPENDS src/*.c*)
add_executable(${PROJECT_NAME} ${CUDAMAIN_SOURCE_FILES})
target_link_libraries(${PROJECT_NAME} PRIVATE my_cuda_lib)
